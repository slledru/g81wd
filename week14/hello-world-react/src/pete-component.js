// 1. Import our necessary libraries
import React from 'react'

// 2. Make the function for our component
const Pete = ({ name, greeting }) => (
  <p>{greeting} {name}, I am Pete!</p>
)

// 3. Export functional component
export default Pete
